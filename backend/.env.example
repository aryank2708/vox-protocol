# Monad testnet
MONAD_RPC_URL=https://testnet-rpc.monad.xyz
CHAIN_ID=10143
EXPLORER_URL=https://testnet.monadexplorer.com

# Backend signer (must have MON for gas). NEVER commit a real key.
PRIVATE_KEY=0xyour_test_private_key_here

# Deployed contract addresses (filled in after `pnpm deploy`)
AGENT_REGISTRY_ADDRESS=
CALL_ATTESTATION_ADDRESS=
REPUTATION_ESCROW_ADDRESS=

# Optional CORS origin for frontend
CORS_ORIGIN=http://localhost:5173
PORT=8787
